#!/usr/bin/env sh
SERVER_HOST=${HOST:='127.0.0.1'}
SERVER_PORT_START=${PORT:=3000}
SERVER_INSTANCE=${INSTANCE:=0}
SERVER_PORT=$((SERVER_PORT_START + SERVER_INSTANCE))
bundle exec thin start -a $SERVER_HOST -p $SERVER_PORT
